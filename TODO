// Vra wat hs in player excell is (stored as: did_highschool:boolean) Moet wees Headshot photo




// Store medical aid in dropdown when adding new Player, split collumns in seperate model

// encrypt id number model(maybe also add all the personal info)

or 

// encrypt everything?

// Profile pic uploads
    >>Delete button to remove on the fly (instead of redirecting to SHOW PAGE)

Validations Player form

Fix sidebar for mobile

Make tables responsive (mobile)

Build teams

Player positions

Fix sidebar

Finish Absences

Finish when games are played > needs JS
